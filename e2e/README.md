# End-to-End Tests (Playwright + Electron)

Drives the **built** Electron app with real keystrokes against an **isolated, offline**
fixture profile, to verify Course-Detail keyboard navigation end-to-end.

## Run

```bash
npm run test:e2e
```

This builds (`tsc && vite build`) then launches Playwright against `dist/`. A failing spec
writes a Playwright `trace.zip` + a screenshot under `test-results/` (both gitignored, and
attached to the `html` report at `playwright-report/index.html`); green runs leave nothing.

## Prerequisites

- **The app has been onboarded once on this machine.** `isAuthenticated` is derived from
  `hasCredential()` (OS keychain, global), so a Canvas token must already exist in the
  keychain. The token's real validity is irrelevant — the mock answers validation. This is
  the **only** data prerequisite — the suite no longer needs a populated `database/canvas.db`
  (it generates its own deterministic seed; see ADR-0011).
- **No populated local DB required.** The suite builds its own schema and seeds a fixed known
  dataset (see "How it works" → seed mechanism). It does **not** copy the dev's real DB, so a
  fresh clone (already onboarded once) runs it green.
- **Closing any running instance is _not_ required** — the e2e app uses its own
  `--user-data-dir`, so it gets a separate single-instance lock.

## How it works

- `mockCanvas.ts` — tiny HTTP server. Answers `GET /api/v1/users/self` with 200 so the
  main process "connects" offline; returns 503 for every other Canvas endpoint so a stray
  auto-sync aborts instead of pruning local data on an empty 200.
- **Seed mechanism (deterministic — ADR-0011).** Two stages:
  - `globalSetup.ts` — runs **once per `npm run test:e2e`**. Launches the built app against a
    throwaway empty cwd so the app's own startup (`MigrationRunner` v1→v113 +
    `verifyAndRepairSchema` + runtime `ensureTable`/`ALTER` self-heal) produces a current,
    complete, **empty** `canvas.db`; polls `schema_version` until it reaches the final
    migration; copies that DB to a stable schema **template**
    (`<os.tmpdir()>/cid-e2e-schema/canvas.db`). The throwaway build dir is cleaned on both the
    success and failure paths. Because the template is generated by the app itself, it never
    goes stale — a future migration flows in automatically.
  - `fixtures/seed.ts` — per test, makes a temp dir, **copies the schema template** (not the
    dev's real DB), writes `.config/canvas-connection.json` pointing Canvas at the mock,
    provisions an isolated `userData/`, then INSERT-seeds a fixed dataset by running
    `fixtures/seedDatabase.js` through an **Electron-as-Node** subprocess
    (`ELECTRON_RUN_AS_NODE=1`, so the Electron-ABI `better-sqlite3` loads cleanly — the
    Playwright runner is plain Node and never touches the addon). `seedDatabase.js` is the one
    unified seed; it imports the duplicate-warning matrix from `seedDuplicateWarning.js`
    (`seedDuplicateMatrix`), which keeps its standalone CLI entrypoint for
    `scripts/manual-test-duplicate-warning.js`.
- `fixtures/app.ts` — Playwright fixture: `_electron.launch(['dist/main.js',
--user-data-dir=…], { cwd: tempDir })`, then presets `localStorage.onboardingCompleted`
  and reloads to land on the main app (skips the first-run welcome guide). Exposes the
  always-present `duplicateSeed` (the matrix is no longer opt-in).
- `skipGuard.reporter.ts` — custom Playwright reporter wired in `playwright.config.ts` that
  **fails the whole run** if any test reports `status === 'skipped'` and is not on an explicit
  `why:`-justified allow-list (currently empty). This is the regression net that keeps
  seed-driven coverage from silently eroding.

Gates cleared: single-instance lock (own user-data-dir), onboarding (keychain), welcome
guide (localStorage preset). DB/config are isolated in the temp cwd; the keychain and the
real `userData` are never modified.

## Coverage

All specs assert via deterministic signals — `sessionStorage` focus index, field `id`
focus, chip count badges, `data-testid` markers, URL hash — never subjective feel. With the
deterministic seed (ADR-0011) every entity a spec needs (queue, announcements, courses,
files, calendar events, sync updates, the duplicate-warning matrix) is **guaranteed present**,
so there are **no data-tolerant `test.skip` guards** — the suite runs skip-free (enforced by
the skip-guard reporter).

16 spec files (33 tests):

- **`smoke.spec.ts`** — boots to the main UI and confirms `getCourses()` returns the
  seeded courses through IPC (the fixture wiring sanity check).
- **`course-detail-keyboard.spec.ts`** (Items 1 & 2):
  - **Item 1** — `Ctrl+E` opens Settings, `Alt+U` → Credits, `Escape` closes the panel.
  - **Item 2** — `W`/`S` walk the focused task; `E` opens edit; overloaded `Alt+G`→Score /
    `Alt+T`→Title resolve correctly; `Ctrl+Enter` saves; digit keys `1–5` switch filters
    (visible rows match the chip count).
- **`course-detail-sections.spec.ts`** (Items 3 & 4 + section cycling):
  - **Item 3 (Queue)** — `Q` cycles to the Queue section (auto-expands); `W`/`S` walk cards.
  - **Item 4 (Announcements)** — cycle to Announcements; `W`/`S` walk; `Enter` opens the
    announcement (URL hash → `#/announcement/…`).
  - **`Q`/`E` section cycling** — exposes ≥2 distinct keyboard-active sections.
- **`announcement-detail.spec.ts`** — Announcement-Detail route + its keyboard scope.
- **`dashboard.spec.ts`** — Dashboard route coverage.
- **`files.spec.ts`** — Files page (reads `resources` type `file`/`page`); hard
  files-present assertion (the seed guarantees ≥1 file).
- **`settings.spec.ts`** — Settings page route coverage.
- **`page-keyboard.spec.ts`** — list-page focus navigation for Courses / Tasks /
  Announcements / Updates (focus engages → moves on next → returns on prev; `Enter` →
  detail route where applicable).
- **`page-nav.spec.ts`** — global page navigation (`Mod+1..5`) across the top-level pages.
- **`section-jump.spec.ts`** — uniform `Alt+1..N` section direct-jump (ADR-0010).
- **`calendar-keyboard.spec.ts`** — `Alt+Shift+D`/`Alt+Shift+P` activate a quick filter,
  `Alt+Shift+C` clears; `Alt+Shift+1` toggles a course filter; plain `Alt+1` is section
  direct-jump (no filter side effect — ADR-0010).
- **Mutation round-trips** (write → IPC read-back, asserting persistence not just an
  optimistic UI flip): **`mutation-add-task.spec.ts`**, **`mutation-archive-course.spec.ts`**,
  **`mutation-setting.spec.ts`**.
- **`modal-stack.spec.ts`** (ADR-0006) — Help shows the topmost modal's `ShortcutCategory`
  (not the page's); arrow-key browser scroll is `preventDefault()`-ed while a modal is open.
- **`modal-stack-duplicate.spec.ts`** (ADR-0006, deep) — DuplicateWarningModal page-leak /
  nested-stack regressions, driven off the always-present duplicate-warning matrix.

### Not yet covered

- The full multi-layer Escape cascade beyond the Settings branch.
- Remaining deeper ADR-0006 surfaces beyond the page-leak / scroll-suppression specs already
  landed (e.g. full Customize-child nested-leak coverage on every host modal) — tracked in
  `docs/FOLLOWUPS.md` if/when prioritized.

## Limitations

Verifies objective behavior (state, focus, navigation, DOM), **not** subjective feel,
exact outline styling, or that `Shift+O`/`O` actually opens a real browser tab (the app
calls `openExternal`; we don't assert the OS side).
