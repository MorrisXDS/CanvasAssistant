#!/usr/bin/env sh

# Block commits that add plaintext credentials (high-precision secret scan).
sh scripts/secret-scan.sh || exit 1

# Cross-platform compatibility checks + format on staged files.
npx lint-staged
